#!/usr/bin/env bash
# Publish repo wiki/ markdown to the GitHub Wiki (not part of `nxr task release`).
set -euo pipefail
root="$(cd "$(dirname "$0")/../.." && pwd)"
exec "$root/scripts/publish-wiki.sh" "$@"
